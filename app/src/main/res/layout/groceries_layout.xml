<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="200dp"
    android:orientation="horizontal"
    android:background="@drawable/card_background"
    android:backgroundTint="@color/groBG"
    android:elevation="3dp"
    android:id="@+id/cardContainer"
    android:layout_margin="5dp"
    android:clickable="true"
    android:layout_height="120dp">

        <ImageView
            android:id="@+id/groceriesProduct"
            android:layout_width="100dp"
            android:padding="5dp"
            android:layout_height="100dp"
            android:layout_marginTop="9dp"
            android:scaleType="fitXY"
            android:layout_marginBottom="10dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/rice1" />
    <TextView
        android:id="@+id/groceriesName"
        style="@style/TextAppearance.AppCompat.Title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimaryText"
        android:layout_marginTop="44dp"
        android:gravity="center_horizontal"
        android:text="Rice"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintLeft_toRightOf="@+id/groceriesProduct"
        app:layout_constraintStart_toEndOf="@+id/groceriesProduct"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>